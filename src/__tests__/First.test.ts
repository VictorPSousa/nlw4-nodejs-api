describe("First", () => {
    it("deve ser possível somar dois números", () => {
        expect(2 + 2).toBe(4);
    });

    it("deve ser possível somar dois números", () => {
        expect(2 + 2).not.toBe(5);
    });
});